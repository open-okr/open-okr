# STATUS.md

The single source of truth for execution progress against IMPLEMENTATION-PLAN.md. The agent updates rows. Only a human sets `done`.

Statuses: `todo`, `in_progress`, `in_review`, `blocked`, `done`, `skipped`. Skipping requires a note and human sign-off. The rules are in EXECUTION-GUIDE.md §5.

**104 tasks.**

## Phase 1: Foundation

| Task | Title | Status | Branch / PR | Updated | Notes |
|---|---|---|---|---|---|
| P1-T01 | Monorepo scaffold | done | task/p1-t01-monorepo-scaffold, PR #1 | 2026-08-05 | Merged to main 2026-08-05. LICENSE was already present (AGPL-3.0). TypeScript pinned to 5.9.x, not 7.x |
| P1-T02 | CI pipeline + environment schema | done | task/p1-t02-ci-pipeline-env-schema, PR #2 | 2026-08-05 | Merged to main 2026-08-05. All ten checks green, including Dependency review after the Dependency graph was enabled. Turbo local cache used instead of a vendor remote cache, no credentials needed. apps/web now depends on packages/config: confirm the TECHNICAL-PLAN §1 table allows it |
| P1-T03 | Database package + tenant floor + test isolation | done | task/p1-t03-db-tenant-floor, PR #9; rework PR #11 | 2026-08-05 | Merged to main 2026-08-05. Spike decision: GO, recorded in docs/design/p1-t03-tenant-isolation-spike.md. Isolation held under PgBouncer transaction pooling; R1 fallback not invoked. No product tables shipped; the probe table is a test fixture, so the §7.2 importer mapping is unchanged. PR #11 fixed GHSA-gpj5-g38j-94v9 (drizzle-orm 0.45.2) and restored the TypeScript 5.9.x pin that dependabot PR #5 broke |
| P1-T04 | Adapter ports + drivers + the transactional outbox | done | task/p1-t04-adapter-ports-outbox, PR #12 | 2026-08-05 | Merged to main 2026-08-05. Eight ports, nine default drivers, the outbox with a leased at-least-once relay, and the boundary gate. Human approved: ws as a runtime dependency; drivers receive a pool by injection so the TECHNICAL-PLAN §1 dependency row stays true; outbox ships not-tenant-scoped and hard-delete markers per §4.13. Also fixed the licence gate, which could not parse SPDX OR expressions. SMTP mail driver deferred: the console driver is the default and nodemailer is a new dependency to approve |
| P1-T05 | Authentication: password, passkeys, one-time codes | done | task/p1-t05-authentication, PR #13 | 2026-08-06 | Session tokens hashed at rest through a wrapper around Better Auth's documented adapter seam; no internals patched, no hand-rolled sessions. Auth lives in packages/core because it needs the database and TECHNICAL-PLAN §1 does not let apps/web reach it. Human approved: @better-auth/passkey; lockout and forgot/reset in scope, single sign-on deferred to P8-T07; auth pages carry S-35 behaviour now and get the S-35 visual treatment in P2-T10. Next 16 renamed middleware to proxy. Two latent bugs found and fixed on the way: the adapter's transaction handle bypassed the hashing wrapper, and the pg-boss dedup test was timing-dependent. Human approved a per-package Dependency review exception for rou3, a transitive Better Auth dependency GitHub reports as MIT AND MS-PL from its repository while the published tarball is MIT only. The allow-licenses list is unchanged: MS-PL stays off it |
| P1-T06 | Workspaces + members bootstrap | done | task/p1-t06-workspaces-members, PR #14 | 2026-08-06 | Merged to main 2026-08-06. Human confirmed: enforce the registration policy now, computed as open while no user exists rather than stored, so system_settings waits for the P1-T09 wizard; no admin role invented, the first member's binding lands at P2-T01; active workspace is a plain cookie revalidated against memberships on every request, so nothing trusts it and no schema is invented; provisioning is one explicit transaction shaped for the P1-T07 Operation lift, with the audit and outbox rows deferred there. A second transaction-local setting, app.user_id, lets a member list their own workspaces inside row-level security instead of bypassing it. Three things found on the way and fixed here: the soft-delete gate had never seen a schema file, so it reported zero tables and checked nothing; pnpm db:migrate was broken by an extensionless import; and §3 asks for time-ordered ids, which Postgres 17 cannot generate, so packages/db now does. The migration linter gained a tenant-root marker rather than letting the workspaces table skip the floor checks as infrastructure |
| P1-T07 | Operation pipeline + action registry + audit spine | done | task/p1-t07-operation-pipeline, PR #15 | 2026-08-06 | Merged to main 2026-08-06. Design recorded in docs/design/p1-t07-operation-pipeline.md. Two readings worth review: authorisation runs inside the write transaction rather than an earlier one, which is stronger than the literal wording of §8.1; and the audit chain takes a per-workspace lock, so writes in one workspace serialise at the end. Found and fixed on the way: the audit verifier reported every chain intact while the tenant floor hid every workspace from it, the same fail-open shape as the P1-T06 soft-delete gate. Human confirmed: authorisation is one seam the pipeline owns, replaced wholesale at P2-T02; the audit chain takes a per-workspace advisory lock, accepting serialised writes for a verifiable chain, with a measurement follow-up on P7-T01; the privilege model moves into packages/db so append-only is stated once; provisioning is lifted into the first Operation and workspace.rename is the second. Carries a P1-T05 follow-up: sign-in lockout is enforced but writes no audit entry, because the audit spine does not exist yet (TECHNICAL-PLAN §8.2 asks for both). Carries a P1-T06 follow-up: workspace provisioning is already one transaction shaped for the Operation lift, and gains its audit row here. It needs no outbox row until something subscribes to workspace creation |
| P1-T08 | Proving dashboard | in_review | task/p1-t08-proving-dashboard, PR #16 | 2026-08-06 | Merged to main 2026-08-06. Awaiting a human to set done. Playwright now runs in CI as its own job. Found and fixed on the way: signing out posted straight at the authentication endpoint, which answers with JSON, so a browser without JavaScript landed on {"success":true} instead of the sign-in screen. It is a server action now. Also observed, not changed: the S-35 screens drive authentication through the client, so they need JavaScript, which is recorded on P2-T10. Human confirmed the recommendation: stand Playwright up now, minimally (Chromium only, one spec, next build and start, reusing the existing role, grant and migration machinery), because the acceptance criterion names client hydration and only a browser proves it, pnpm test:e2e is documented but absent, and P1-T09 needs the same machinery against a far harder target. The e2e harness takes a fresh database per run from the start, because the registration policy closes an instance after its first user. Three states are real on this route and the fourth is not: a signed-in member always has a workspace, so there is no empty state to build |
| P1-T09 | Docker Compose target + first-run setup wizard | done | task/p1-t09-compose-setup-wizard, PR #17; mail seam PR #18 | 2026-08-06 | Merged to main 2026-08-06 across two PRs. PR #17 merged one commit early, so the review fixes landed separately as PR #18; verified on main rather than from the PR state, which is the check that caught it. Measured against the acceptance criterion on real Compose: 17s from nothing to a healthy instance against a 30-minute budget, with an admin created and a workspace provisioned. Image is 204MB, down from 726MB once the migration runner stopped shipping the build's node_modules to run a few hundred lines of SQL; Next already traces pg into the standalone output. A deploy CI job builds the image, boots the stack and drives the wizard, and eleven checks in deploy/docker/smoke-test.sh cover the budget, migrations on boot, the wizard redirect, honest connection reporting, proxy headers, admin creation and upgrade idempotence. Two more gates that failed open, found by running the software: the migration lint announced success without saying what it read, so a renamed directory would have made it a permanent pass; and my own ./openokr up reported ready while the proxy crash-looped behind a healthy application. Both now state their coverage. The container's database check gained a third exit code for the same reason, and it immediately diagnosed a stale volume outliving regenerated secrets, which is now refused with an explanation rather than producing an instance that cannot authenticate. Deliberately partial: the wizard does not offer demo data, because demo data means objectives, key results and a cycle and those arrive in Phase 3, so a checkbox that seeded nothing would be worse than its absence; recorded on P3-T01. Human confirmed four decisions before code: Caddy for the reverse proxy, because automatic certificates are built in rather than a second process with its own renewal timer; nodemailer approved as the SMTP driver, unblocking the P1-T04 deferral and the wizard's mail test; local disk stays the only storage service, with S3-compatible storage reachable by environment variable, so a default install is three containers and Postgres remains the only required service; and the wizard tests what exists rather than faking the rest, so channel and AI report that they are not in this build instead of showing a green tick for an untested connection. Two readings recorded here: the wizard's pages live in apps/web/app/setup because a web wizard must be served by the application, while deploy/docker holds the image, compose, proxy configuration, entrypoint and lifecycle helper; and there is no S-xx screen for instance setup, S-34 being the in-app onboarding after sign-in, so this is built to the UIUX-PLAN §4 patterns and §9 gates without a numbered screen. Envelope encryption and system_settings land here rather than later, because the wizard stores SMTP credentials and there is no acceptable interim where those sit in plaintext. A post-implementation review before P1-T10 found the mail work stopped one seam short: the SMTP driver existed but nothing could construct it, the OPENOKR_MAIL_* variables did nothing, reset mail always went to the console, and the wizard probe ignored the environment. Fixed with a createMailer factory, resolveMailSettings in core, per-send mailer construction, and a probe that tests the resolved configuration live. The same review closed finishSetup to unauthenticated callers and bounded its input |
| P1-T10 | Helm chart + Phase 1 exit | in_review | task/p1-t10-helm-phase-exit, PR #19 | 2026-08-06 | Merged to main 2026-08-06; verified on main rather than from the PR state. Awaiting a human to set done. A post-merge review before Phase 2 found eleven more things and fixed six of them on branch chore/p1-hardening: see the Phase 1 hardening section below. Verified on a real kind cluster, not from templates: 12 checks covering install, both replicas ready, the migration hook running before the pods, registration through the installed instance, and an upgrade that does not rotate the encryption key. Plus 23 chart checks that need no cluster. Three real bugs found by running it, all invisible to helm lint: a pre-install hook cannot reference the chart's ServiceAccount or its Secrets, because Helm creates every pre-install hook before any normal resource, so the first install failed twice; and the generated encryption key was 32 characters rather than 32 bytes, which the key ring refused at boot exactly as designed. The Phase 1 exit checklist is recorded in docs/design/p1-exit-checklist.md with evidence per line and four caveats written down rather than ticked: the action registry is real but holds two actions, authorisation is a seam with no bindings until P2-T01, mail has never reached a real mailbox, and instance settings writes are a stated exception to the Operation pipeline. Human confirmed four decisions before code: CI installs the chart into a real kind cluster and registers a user, because a chart can lint perfectly and fail on install and this repository has been bitten four times by gates that pass while checking nothing; the chart deploys no database and requires an external one, matching PLAN §121 and avoiding a third-party subchart whose backups nobody owns; images publish to ghcr.io on a version tag and are signed with cosign keyless, so there are no signing keys to store or rotate; and the chart generates its secrets on first install, reads them back on upgrade so a helm upgrade never rotates a key underneath stored data, and accepts an existingSecret for operators with their own secret manager |

### Phase 1 hardening (between P1-T10 and P2-T01)

Not a numbered task. A review of the whole of Phase 1 after P1-T10 merged, on
branch `chore/p1-hardening`, fixing what would be more expensive to fix once
Phase 2 built on it. Six fixes, each with a test that fails without it.

| # | What was wrong | Fix |
|---|---|---|
| 1 | The Operation pipeline applies `app.workspace_id` and `app.user_id` together, and Postgres combines permissive policies with OR, so every read inside every operation also returned the acting user's rows from every other workspace. A test asserted that union and called it correct | Migration 0008 scopes the two cross-workspace policies to transactions that name no workspace, and aligns their soft-delete predicates. The switcher and the provisioning membership check both use `withUser`, which sets no workspace, so both are unchanged |
| 2 | `./openokr rotate-key` had never worked. It ran a file the image did not contain, and `NODE_PATH` was exported by the entrypoint alone so nothing reached it through `docker compose exec` | The migrator layer now carries the core and config sources with the workspace links rebuilt, plus the two packages Next compiles away rather than tracing out. The smoke test rotates the key and then proves the instance still reads its own data. Image 205MB to 235MB |
| 3 | `OPENOKR_DOMAIN=... ./openokr up` wrote `BETTER_AUTH_URL=http://localhost` and kept it forever, because the helper read `OPENOKR_URL`, which nothing sets, and secrets are generated once. Passkeys bind to that origin | The origin follows `OPENOKR_DOMAIN`, and is reconciled on every `up` rather than only on the run that generated the secrets. It says when passkeys will need re-enrolment |
| 4 | The outbox rule did not cover `apps/`, and a direct mail send was already sitting there. One mention of `runOperation` exempted an entire file. Raw SQL writes and side effects reached through a call expression were both invisible | The rule covers `apps/`, exempts only writes lexically inside a pipeline call, and sees `tx.execute(sql\`update ...\`)` and `mailerFrom(s).send(...)`. It immediately found two real writes in provisioning, now marked with the operation they run inside |
| 5 | The migration linter accepted a policy on `invitations` as a policy on `invitation`, read `-- openokr:hard-delete is deliberately absent` as the marker being present, allowed `using (true)` on a workspace table, and skipped a directory it could not find | All four closed. Directories are now counted individually, so a renamed migrations directory fails instead of passing on the fixtures alone |
| 6 | SMTP error redaction covered the password and `base64(password)`, but AUTH PLAIN sends `base64(NUL user NUL pass)`. Whether the password survived depended on byte alignment, so it leaked for roughly two usernames in three | Every encoding is redacted, and any base64 left standing is decoded and checked. Proved against a server that quotes the rejected AUTH line back |

Also: CodeQL scoped away from the reference HTML that accounted for all forty
open alerts and none of the product code; `engines` pinned to `>=22 <23`
because every entry point runs `--experimental-strip-types`; six commands that
CLAUDE.md documented but that do not exist moved into a table naming the task
that delivers each; the release workflow gained the sign-off, chart and compose
gates it was missing and lost an `id-token` permission a verifier should not
hold; the compose smoke test stopped pinning the migration count; and the four
placeholder package tests now say they are wiring checks rather than coverage.

Five more findings are recorded on the Phase 2 rows they belong to rather than
fixed here: realtime reconnection and the outbox dead-letter path (P2-T06),
cache eviction (P2-T09), the port lifecycle contract (closed early, see the
cross-phase gap review below, before P2-T13 needed it), the job queue
restart (P2-T17), and local-disk content types (P2-T05).

### Cross-phase gap review (between P2-T12 and P2-T13, 2026-08-10)

Not a numbered task. The human asked for an audit of gaps between Phase 1 and
Phase 2, then asked for all of them to be fixed directly rather than left for
whichever later task would otherwise have carried them. On branch
`chore/close-phase1-gaps`. Four fixes.

| # | What was wrong | Fix |
|---|---|---|
| 1 | The port lifecycle contract had no agreement: `JobQueue` and `Realtime` declared `stop()`, the other six ports declared nothing, `SmtpMailer.close()` existed but was not on the `Mailer` port, and `Adapters.close()` only ever called two of eight drivers. Flagged since P1-hardening and carried on the P2-T13 row as "settle the contract before a ninth driver with long-lived HTTP clients joins" — which is exactly what P2-T13's AI provider drivers are | Every port (`Cache`, `Mailer`, `Search`, `Channel`, `FileStorage`, `AIProvider`, plus the two that already had it) now declares `stop(): Promise<void>`. Six drivers that own nothing to release no-op it; `SmtpMailer.stop()` wraps its existing `close()`. `Adapters.close()` calls all eight. A driver added later — the first AI provider driver among them — is shut down by construction rather than by remembering to wire it in |
| 2 | Sign-in lockout has been enforced since P1-T05 without an audit entry, first because the audit spine did not exist (closed at P1-T07), then because `audit_events.workspace_id` is `not null` and a failed sign-in has no workspace to attach to — a fact about an email address and a caller address, resolved before any membership is known, that can implicate zero, one or several workspaces. Carried through P1-T07's row, then P2-T09's, each time deferred to "the P8-T03 open question" | A second, parallel chain: `instance_audit_events` (migration 0014), hash-chained the same way `audit_events` is but with no `workspace_id` and one sequence for the whole instance, read and written through the same `app.instance_admin` transaction-local flag `system_settings` already uses. `packages/core/src/audit/instance-chain.ts` is its hashing and append logic, parallel to `audit/chain.ts` rather than sharing it, because an instance row has no actor, target or workspace for that type to require. Better Auth's own rate limiter rejects a request from inside its router, before any hook, `onAPIError` or `onResponse` callback ever runs, so there is no extension point inside it that ever sees the 429 it produces (confirmed by tracing `better-call`'s router, not assumed); `apps/web/app/api/auth/[...all]/route.ts` wraps the handler instead and writes the entry when the response is a 429. Deliberately not recorded: the attempted email address, on §8.2's own "minimal personal data in logs" line — the path and the caller's address already show a lockout happened, where, and from where |
| 3 | TECHNICAL-PLAN §1's package dependency table never listed `config` for any package, but `packages/core`, `packages/db` and `apps/web` all already reach it (mostly from command-line entry points: `migrate.ts`, `data-change.ts`, `verify-audit.ts`, `rotate-keys.ts`, plus `apps/web`'s own boot check). First noticed at P1-T02 ("confirm the TECHNICAL-PLAN §1 table allows it") and never confirmed in the four tasks since | Added a note under the table: `config` sits underneath it rather than inside it, available to every package the way `zod` is, because it is the shared environment and lint schema rather than a domain dependency. No code changed; the table was stale, not the imports |
| 4 | Two stale task citations, found while fixing the above and while working on P2-T12: IMPLEMENTATION-PLAN.md's P2-T16 cited `AI-NATIVE-PLAN.md §1.7`, which does not exist (§1 has no subsections); CLAUDE.md's "commands that do not exist yet" table attributed `pnpm gen:contract` to P2-T09, which is unrelated (security baseline), when `packages/core`'s own registry names P5-T07 onward | Corrected both directly, with the human's explicit go-ahead. §1.7 is now §4 ("Budgets and limits"); `gen:contract` now cites P5-T07 (REST/OpenAPI/CLI, where the drift check it depends on is built), noting P5-T06 and P5-T09 as the chat-command and MCP-tool-catalogue projections of the same registry. Committed separately as `chore/fix-ai-native-plan-citations`, ahead of this row |

Two items from the audit were surfaced but deliberately not treated as bugs to
fix: P1-T08 and P1-T10 are still `in_review`, not `done` — CLAUDE.md reserves
setting a task `done` for a human, so this is not the agent's row to change,
only to flag. And mail has never been tested against a real SMTP server
(carried since the P1-T09 exit checklist) — verifying that needs either a
live external account or a local test SMTP catcher, neither of which this
sandbox can add or prove without a human decision on the dependency.

**Verified** under Node 22: typecheck (`packages/adapters`, `packages/db`,
`packages/core`, `apps/web`), Biome, the boundary gate, the migration linter
and the soft-delete lint all pass. `packages/config`'s own suite and, for the
first time, `apps/web`'s own Vitest suite (it needs no database, unlike
`packages/core`/`packages/db`) both ran and passed here, 16 tests including
four new ones proving the lockout-audit wrapper: a 429 gets exactly one audit
call with the right path and address, an ordinary response gets none, a
failing audit write still returns the 429 rather than a 500, and the address
falls back from `x-forwarded-for` to `x-real-ip` to a placeholder. **Not
verified:** `packages/core/test/instance-audit.test.ts` and the `stop()`
additions to `packages/adapters/test/{drivers,smtp}.test.ts` — no
Docker/Postgres in this sandbox for those two packages, same gap as every
prior Phase 2 row.

## Phase 2: Platform and agent spine

| Task | Title | Status | Branch / PR | Updated | Notes |
|---|---|---|---|---|---|
| P2-T01 | Access model: contexts, bindings, groups | in_review | task/p2-t01-access-model | 2026-08-09 | Closes both P1-T06 and P1-T07 follow-ups: workspace provisioning now gives the workspace its own access context and `workspace_standard` group, and gives the first member a `member` group with a `full` binding on that context, atomically in the same transaction. Four tables (`access_contexts`, `access_groups`, `access_group_memberships`, `access_bindings`), migration 0009, all with the tenant floor and RLS. `access_group_memberships` is schema-only for now: nothing writes to it until `space_standard` groups exist at P3-T01, because workspace-wide membership is read off `workspace_members` directly rather than enumerated. `derivePrivacy` (packages/core/src/access/privacy.ts) is pure and untested against a stored value by design, matching "never a stored boolean". `can()` and the access-aware getter that actually read these tables are P2-T02; every active member still resolves to `full` until then. Reusable `ensureContext`/`ensureWorkspaceStandardGroup`/`ensureMemberGroup`/`bindGroup` helpers in packages/core/src/access/contexts.ts are for later Operations to call when they create their own aggregate; provisioning is the only caller today. Verified: typecheck, Biome lint, the migration linter (10 files), the boundary gate (130 files) and the soft-delete lint (6 tables, 86 files) all pass under Node 22, run directly since this sandbox's default Node is 20 and pnpm's own corepack shim would not run at all. Not verified: the actual test suite. This sandbox has no Docker and no reachable Postgres, and every package's Vitest project opens a real database in global setup, so packages/core/test/access-model.test.ts and the existing provisioning suite could not be executed here; `derivePrivacy`'s cases were checked by hand outside Vitest instead. A human needs to run `pnpm db:up && pnpm test` before merging |
| P2-T02 | can() + access-aware reads | in_review | task/p2-t02-can-access-reads | 2026-08-09 | Closes both inherited follow-ups. `resolveActor` in operation.ts now resolves the member's real level on the workspace's own context instead of hardcoding `full`; `ResolvedActor.level` is a plain number rather than `AccessLevel` because zero is a real answer. `workspace.overview` now calls `getAccessScoped` to authorise, with one remaining raw read of its own display fields marked `openokr:allow-raw-read`. Built: `resolveMemberAccessLevel` (member's own group, workspace_standard, and any space_standard group they hold a live `access_group_memberships` row in — maximum wins), `resolveAnonymousAccessLevel` (the anonymous group alone, kept separate rather than making `can()`'s memberId optional), `can()`, `getAccessScoped` (not-found on missing resource, insufficient level or suspension alike), `accessScopeFilter` (a composable EXISTS clause for listing), and the fail-closed `resolveSubjectContext` with one entry so far (`workspace`). Extracted `OperationError` into its own module (`operations/errors.ts`) to break a circular import between operation.ts and the new access/reads.ts. Extended `packages/config/src/boundaries.ts` with a fourth rule, `protected-read-outside-getter`, covering `workspaces` today; found and fixed a real bug in the same change while wiring it up: `statementStartLine`, reused from the mutation rule, stops at the first `{`/`}` walking backward, which is the closing brace of a preceding multi-line `.select({...})` argument rather than the statement's own start, so the escape marker has to sit directly above `.from(...)` for this rule rather than above the statement. A regression test locks in the correct placement. Added a test fixture table, `access_probes` (mirroring `tenant_probes` from the P1-T03 spike), to exercise `accessScopeFilter` against several rows at once; never shipped. **Open question for a human:** the guest kind inherits the `workspace_standard` tier exactly like a human member today, because nothing in TECHNICAL-PLAN §4.1 or P2-T01 excludes it. Is that correct, or should a guest only ever reach what a personal binding names? Recorded and tested as current behaviour in packages/core/test/access-reads.test.ts, not silently assumed. **Not verified:** the Vitest suites. This sandbox has no Docker and no reachable Postgres; typecheck, Biome, the boundary gate, the migration linter and the soft-delete lint all pass under Node 22 (this repo's own default Node in this sandbox is 20, under which pnpm itself will not run), but packages/core/test/access-reads.test.ts and the wider suite need `pnpm db:up && pnpm test` before merging |
| P2-T03 | People: profiles, manager chain, lifecycle | in_review | task/p2-t03-people-lifecycle | 2026-08-09 | No new migration: `workspace_members` already carried every field this task needed since P1-T06. Built `packages/core/src/people/manager-chain.ts` (cycle detection, possible-managers, the org-chart tree, all depth-capped rather than trusting the invariant unconditionally) and `people/lifecycle.ts` (the last-owner check, `stripBindings` for guest conversion), registered as actions in `actions/people.ts`: `updateOwnProfile`, `updateMember`, `suspend`, `restore`, `convertToGuest`, `erase`, `directory`, `orgChart`, `possibleManagers`. Suspension needed no binding changes at all: `resolveMemberAccessLevel` and `resolveActor` already exclude a non-active member (P2-T02), so suspend and restore are exactly the status flip and nothing else. **Two readings recorded for a human to confirm, neither assumed silently:** (1) nothing in TECHNICAL-PLAN §4.1 names an owner role, so "the last owner" is read as *the last active member holding `full` on the workspace's own context*, checked by walking every other active member's resolved level rather than counting bindings, since a member can reach `full` through more than one tier. (2) The self-versus-others split for profile fields is this task's own line: self edits timezone, avatar and bio (personal); an admin with `full` edits name, title and manager (org-structural). Erasure sets the row to a placeholder name with `bio`/`title`/`avatar`/`timezone`/`userId` cleared and `status` forced to `suspended`, keeping the row itself as the stable identity later authorship still points at, and returns the prior profile as the operation's own result rather than writing it anywhere. **Deliberately not built:** the S-33 screen itself (directory UI, profile page, org chart rendering). IMPLEMENTATION-PLAN lists Depends on: P2-T02 only, but there is no app shell or design system to build a real page into yet (P2-T10); building one now would be throwaway, the way the workspace switcher's UI was deferred from P1-T06 to P2-T10 for the same reason. The three read actions (`directory`, `orgChart`, `possibleManagers`) exist and are tested; only their rendering is deferred, and this row is that follow-up. Bio remains unvalidated JSON: the shared rich-text validator TECHNICAL-PLAN §4 requires is P2-T11, not yet built, so `bio` is accepted loosely rather than ahead of that module. **Verified** under Node 22 (see P2-T01/P2-T02 rows for why): typecheck, Biome, the boundary gate, the migration linter and the soft-delete lint all pass. **Not verified:** packages/core/test/people.test.ts, same Docker/Postgres gap as every other Phase 2 row so far. Needs `pnpm db:up && pnpm test` before merging |
| P2-T04 | Invitations | in_review | task/p2-t04-invitations | 2026-08-10 | Closes the P1-hardening follow-up: `invitations.createPersonalLink` is the first caller to actually go through the outbox rather than a direct send, topic `invitation.email`. Migration 0010 adds `invite_links` (mode `workspace`/`personal`, SHA-256 token hash, `email` set only for `personal`, `allowed_domains`, `use_count`/`max_uses`, `expires_at`, `revoked_at`, `member_id` filled in at acceptance). All three joining paths (`invitations.acceptLink` for both link modes, `invitations.joinByTrustedDomain` against `workspaces.settings.trustedEmailDomains`, already resolvable since P1-T06) funnel through one `provisionMemberForInvite` in `packages/core/src/invitations/provisioning.ts`, idempotent on an existing live membership. Both acceptance actions are `bootstrap` operations — the accepting person has no member row yet — which widened the doc comment on `OperationSpec.bootstrap` in operation.ts from "creates the workspace" to "creates the workspace, or creates this member's membership in it", since it now has two callers instead of one. **Two things recorded for a human, neither decided silently:** (1) no invite carries its own access level (TECHNICAL-PLAN's `invite_links` row has no such column), so every joining path grants the same default (`edit`) `defineWriteAction` itself defaults to; a per-invite level is a natural follow-up once someone needs to invite at `view` or `comment` instead. (2) The raw invite token has to travel through the outbox payload for the email to be sendable at all, since the column stores only its hash and the raw value exists nowhere else after this transaction. Outbox rows are not deleted after delivery today, so the raw token stays readable in the table past the point the invite is used or expires — worth a look at P7-T03, not fixed here since it would mean inventing outbox retention this task wasn't asked to build. Also fixed on the way: the migration's first draft indexed `token_hash` globally, on the reasoning that a token might be looked up before its workspace is known; it doesn't, an invite URL names its workspace (by slug) alongside the token, so acceptance runs with the tenant floor already applied like any other request, and the index is workspace-scoped like every other one in this schema. **Verified** under Node 22: typecheck, Biome, the boundary gate, the migration linter and the soft-delete lint all pass. **Not verified:** packages/core/test/invitations.test.ts — no Docker/Postgres in this sandbox, same gap as every prior Phase 2 row. Needs `pnpm db:up && pnpm test` before merging |
| P2-T05 | Files and blobs | in_review | task/p2-t05-files-blobs | 2026-08-10 | Closes the P1-hardening follow-up: removed `LocalDiskStorage`'s process-local `#contentTypes` map outright, since nothing ever read it. Content type is now sourced durably from the `blobs` table (migration 0011), which is what should have been holding it all along. Migration adds `status` value `pending` beyond the three TECHNICAL-PLAN names (`ok`/`scanning`/`quarantined`): the gap between prepare and claim, and what the (unwired) orphan cleanup query targets — flagged as an extension of the terse spec, not a silent change. New workspace setting `storageQuotaBytes` (§4.14), default 5 GiB; TECHNICAL-PLAN names no figure. Each blob is its own protected aggregate (`resourceType: "blob"`), with the uploader given `full` through their own member group at prepare time, and `blobs.getForDownload` resolves it through `getAccessScoped` rather than trusting `author_member_id`. "Exactly one warning" is read as one per crossing of ninety percent, not one per upload while already over it: `checkQuota` compares the total before and after this specific upload. **Deliberately not built, and flagged for a human rather than silently skipped:** image re-encoding, a thumbnail worker and a virus-scan hook all need a new runtime dependency (an image library at minimum) that CLAUDE.md requires asking about first. `claimBlob` accepts an optional `requiresScan` flag that moves a claim to `scanning` instead of `ok`, so the state machine will not need to change shape once a scanner exists — scaffolding, not a built capability. The orphan cleanup job itself is the same story as P2-T04's outbox dispatcher: `findOrphanedBlobs`/`discardOrphanedBlob` exist and are tested, but nothing schedules them yet; that needs the job queue wiring no task has built. Also carries the upload-side gap the code itself notes: a claim refused for exceeding the quota leaves bytes already sitting in storage, since this module stays adapter-agnostic and cannot call `FileStorage.delete` itself — the app route that drives an upload, or the orphan job once it exists, has to clean that up. **Verified** under Node 22: typecheck, Biome, the boundary gate, the migration linter and the soft-delete lint all pass. **Not verified:** packages/core/test/blobs.test.ts, and packages/adapters' own suite (which also needs Postgres, discovered while trying to at least run that one) — no Docker/Postgres in this sandbox. Needs `pnpm db:up && pnpm test` before merging |
| P2-T06 | Subscriptions + notification spine | in_review | task/p2-t06-notification-spine | 2026-08-10 | Closes both inherited P1-hardening follow-ups first, as scoped. `PostgresRealtime`: a rejected `#connecting` promise is no longer memoised across a failure (it used to stay assigned via `??=` forever), and a dropped connection (`error` or `end`) now clears state and re-issues `listen` for every channel with a live subscriber on the next call, rather than silently stopping delivery. `OutboxRelay`: a new `maxAttempts` option (default 10) dead-letters a row instead of retrying it forever, `listDeadLettered()` gives the visibility `last_error` alone never did, and an `onDeadLetter` callback fires the moment one is given up on. New migration 0012 adds `outbox.dead_lettered_at`. Both fixes have regression tests: a mocked one-time connect failure proving the same instance recovers with no new options, and a poisoned row proving it stops competing for the relay's attention after the ceiling. Migration 0013 then builds the machinery itself: `subscription_lists`/`subscriptions` (domain J, ahead of comments/reactions which are Phase 3) and `notifications`/`notification_settings`/`notification_batches` (domain K). Two extensions to TECHNICAL-PLAN's terse column lists, both flagged rather than silent: `notification_batches.status` gains `failed` beside `pending`/`sent`, and `notifications` gains `snoozed_until` for the inbox's own snooze action. **Recipients resolve from subscriptions only** — "and role obligations" is not built, because no role tag (champion/reviewer/sponsor/facilitator/coordinator) carries a notification meaning yet; recorded here for whichever task gives one one. Access is re-checked per recipient through `getAccessScoped` at resolution time, not assumed from the subscription row, so someone who lost access after subscribing is silently dropped rather than notified about something they can no longer see. Batching's row lock is a partial unique index (`(workspace_id, member_id, channel) where status='pending'`), not an explicit `SELECT ... FOR UPDATE`; tested by racing three genuinely separate transactions rather than three calls sharing one, which would never exercise the losing path. Settings default to TECHNICAL-PLAN §11's worked example (mentions immediate, everything else batched in 30 minutes, daily summary at 08:00 local) and are created lazily on first touch. **Deliberately not built, flagged rather than silently dropped:** nothing dispatches a `pending` batch or an unsent immediate row (no send worker exists yet); nothing calls `notifyRecipients` from an activity (P2-T07's own deliverable, "notification fan-out driven from activities"); the daily summary has no scheduler; and there is no development preview page for the two mail templates, since apps/web has no shell yet (P2-T10). **Verified** under Node 22: typecheck, Biome, the boundary gate, the migration linter and the soft-delete lint all pass. `packages/config`'s own Vitest suite re-ran clean (unaffected, but re-checked since this task touched `packages/adapters`). **Not verified:** `packages/core/test/notifications.test.ts` and the new tests in `packages/adapters/test/{realtime,relay}.test.ts` — no Docker/Postgres in this sandbox; `packages/adapters`' whole suite needs it too, discovered while trying anyway. Needs `pnpm db:up && pnpm test` before merging |
| P2-T07 | Typed activity feed engine | in_review | task/p2-t07-activity-feed-engine | 2026-08-10 | Closes the P1-T07 follow-up: `kind` is no longer a free string. `runOperation` itself now validates every activity's kind against `packages/core/src/activities/catalogue.ts` (19 registered kinds, one Zod schema each) before the insert, so an unregistered kind or a mismatched payload fails the whole write, not just a lint. This is a change to the shared pipeline every prior Phase 2 task's actions and tests run through, so it had to not break any of them: `test.*`-prefixed kinds (used throughout every task's tests as pipeline scaffolding) are exempt by design, and the 19 real kinds already in use were catalogued from what those actions actually pass, retrofitting `name` into five `people.ts` payloads (suspend/restore/convertToGuest/erase/updateMember) that had no human label at all before this — a feed entry reading "member.suspended" with an empty payload is not readable, "Jane Doe was suspended" is. `context_id` now fills automatically via a new `resolveActivityContext` that wraps the P2-T02 resolver but swallows its raise into `undefined`: the access-check resolver correctly fails closed by raising, but raising here would abort a write over a feed column, so this fails toward invisible-in-feeds instead. Feed queries (`queryFeed`) treat a null `context_id` as workspace-public and a live one as access-scoped through `accessScopeFilter`, paginate by `(at, id)` key, and check subject liveness only for the two subject types this package has a table for (`workspace_member`, `blob`) — every other type is unfiltered on that axis, the same "ahead of its consumers" gap the P2-T02 subject resolver already has. Aggregation (`aggregateFeed`) only collapses kinds in `AGGREGATABLE_KINDS` (today: the two profile-edit kinds); every other kind, including every member lifecycle transition, is narrative and never collapsed even when repeated back to back. Notification fan-out (`fanOutActivity`) is wired into the pipeline behind a new opt-in `ActivityInput.notify` flag rather than firing for every activity automatically — no existing Phase 2 action sets it, since none of today's subject types have real subscribers yet, so it is proven directly against a synthetic subscription in the test suite rather than by flipping it on an existing action for cosmetic effect. Live inserts and rendering are both adapter-agnostic core output: `toLiveActivityEvent`/`workspaceFeedChannel` hand back the shape and channel name for whichever app layer holds the Realtime port to publish, and `renderActivity` is a plain per-kind string builder, not the rich-text renderer, because every value it weaves in is already-trusted stored data, not member-authored content. **Verified** under Node 22: typecheck, Biome, the boundary gate, the migration linter and the soft-delete lint all pass; `packages/config`'s own suite re-ran clean. **Not verified:** `packages/core/test/activities.test.ts` — no Docker/Postgres in this sandbox, same gap as every prior Phase 2 row, and this row carries the most risk of any of them given how central `operation.ts` is: run `pnpm db:up && pnpm test` for the *whole* `packages/core` suite, not just this file, before merging |
| P2-T08 | Workspace settings + module registry | in_review | task/p2-t08-workspace-settings-module-registry | 2026-08-10 | Found and fixed a real defect in the shared pipeline while building this: `defineWriteAction`'s handler never passed the action's declared `access` level to `runOperation` as `requires`, so every write action's actual enforcement silently defaulted to `edit` regardless of what it declared — an `edit`-level member could already reach every `full`-declared action across Phase 2 (`workspace.rename`, `people.suspend`/`restore`/`convertToGuest`/`erase`, both invitation link actions), and conversely two `view`-declared blob actions were over-enforced at `edit`. Fixed with one line (`requires: spec.requires ?? definition.access ?? ACCESS_LEVELS.edit`), letting an `operation()` callback's own `requires` still win for a resource-level rule finer than one flat number. No test actor in the existing suites is below `full` on a `full`-declared action, so this tightens/loosens enforcement without changing any existing test's expected outcome, verified by reading every `callAction`/`.handler(` call site across the test suite rather than assumed. No migration: `workspaces.settings` already existed. Extended `SETTINGS_REGISTRY` with a Zod `schema` per setting (what an admin write validates against) and an optional `card` (the S-36 admin card a workspace-scoped setting resets with). Closed a real gap along the way: the workspace `language` default was reading a hardcoded `INSTANCE_DEFAULT_LANGUAGE` module constant that could never see `OPENOKR_DEFAULT_LANGUAGE` or an administered override; `createWorkspace` now resolves it through the instance's own already-built `instance.language` setting chain (database, then environment, then the constant), which is what "validated storage where environment overrides win" in the task's own deliverable line turned out to mean once traced through TECHNICAL-PLAN §4.14's actual environment story — instance settings bootstrap from the environment, workspace settings inherit that resolution rather than a separate one of their own. New actions in `actions/settings.ts`: `settings.updateWorkspaceGeneral` (timezone/language/trustedEmailDomains), `settings.updateWorkspaceBranding`, `settings.readWorkspaceSettings`, and `settings.resetWorkspaceSettings` (by key or by whole card, resolving each setting's default with no context, since an admin screen has no browser signal to offer the way registration does). The workspace's `name`/`slug` stay on the existing `workspace.rename` action rather than gaining a second writer. Three new activity kinds (`workspace.general_settings_updated`, `workspace.branding_updated`, `workspace.settings_reset`), the first two aggregatable. The module registry (`packages/core/src/modules/registry.ts`) is a flat, DB-free, growable list of navigation items each declaring a required access level, with `navigationFor` (filter for display) and `isRouteAllowed` (the same check for denial) sharing one comparison, so "hidden" and "denied" cannot drift apart. `apps/web` wires it into a real, unstyled two-level admin shell: `app/admin/layout.tsx` gates the whole subtree at `full` through a new `lib/access.ts` (built on a new core export, `resolveOwnWorkspaceAccessLevel`, a `Pool`-based sibling of `resolveMemberAccessLevel` for a caller outside the Operation pipeline) and renders the section list the registry allows; `/admin/general` and `/admin/branding` are the two cards that exist today, each a plain form calling the actions above plus a reset button; the proving dashboard now shows the same admin links, filtered the same way, so "hidden below the level" is visible in the running app and not just in a unit test. Every other S-36 card (members and access, spaces, rhythm, thresholds, coaching, nudges, notifications and channels, authentication, the audit log, the freeze switch, backups, import and export) is deliberately not built: each arrives with the module that owns its storage, the same way `SETTINGS_REGISTRY` itself grows. **Verified** under Node 22: typecheck (`packages/core`, `packages/db`, `apps/web`), Biome, the boundary gate, the migration linter and the soft-delete lint all pass; `packages/config`'s own suite re-ran clean. **Not verified:** `packages/core/test/{settings,settings-actions,modules,provisioning}.test.ts` — no Docker/Postgres in this sandbox, same gap as every prior Phase 2 row, and this row carries extra risk given the `define.ts` fix touches every write action's enforcement: run `pnpm db:up && pnpm test` for the *whole* `packages/core` suite, not just the new files, before merging. The UI has not been exercised in a browser either, for the same reason P2-T06's mail templates and P2-T03's directory screen were not: no design system yet (P2-T10), and no Docker here to run the dev server against a real database |
| P2-T09 | Security baseline | in_review | task/p2-t09-security-baseline | 2026-08-10 | Closes the inherited P1-hardening follow-up: `InProcessCache` now caps itself at a configurable `maxEntries` (default 10,000), evicting the oldest-inserted key once full. A `rateLimit` counter for a subject that never returns is bounded rather than immortal — "grows forever" becomes "grows to here and stops" — proven with a tiny cap in a dedicated test rather than inserting ten thousand real entries. The freeze overlay (§4.1, §8.2) is now real: `runOperation` loads `workspaces.state` ahead of everything else in every write and refuses with `forbidden` when it is not `active`, except for an action on the recovery list (`packages/core/src/operations/freeze.ts`) — every `people.*` and `settings.*` action, plus the new `workspace.setState` itself, so a `full`-access admin can always lift a freeze. `read_only` and `frozen` collapse identically, matching TECHNICAL-PLAN §4.1's own wording exactly ("not active" is the only distinction it draws). The recovery list's membership is this task's own reading of the acceptance line's "members and settings," flagged rather than assumed uncontroversial — TECHNICAL-PLAN names no exhaustive list. A workspace row that does not exist yet (`workspace.provision`, mid-transaction) is silently unchecked rather than a special case keyed on `bootstrap`: the same "load and see if it's there" logic handles it because `activeOnly` on a not-yet-inserted id simply returns nothing. One side effect worth naming, not hiding: accepting an invitation into a frozen or read-only workspace is now refused too (`invitations.acceptLink`/`joinByTrustedDomain` are bootstrap operations but the workspace they join already exists), which was not a question this task set out to answer but falls out correctly from the mechanism rather than needing its own branch. `apps/web/proxy.ts` (pre-existing from P1-T05, gating on a session cookie) now also sets a strict content security policy with a fresh nonce every response, plus `X-Frame-Options`, `X-Content-Type-Options`, `Referrer-Policy` and `Strict-Transport-Security`, following Next's own documented proxy-based nonce pattern exactly — every page under it is already dynamically rendered (`requireSession`/`requireWorkspace` call `headers()` first), which nonce-based CSP requires. `apps/web/app/account/security` gains a session list with revoke (`sessions.tsx`), calling Better Auth's own core `listSessions`/`revokeSession` endpoints directly (no plugin, no migration — they already existed, just uncalled) through a server action rather than the browser SDK, matching this page's existing pattern. Proven end to end in `packages/core/test/auth.test.ts`: two devices signed in, one revoked by token from the other, and the revoked device's next `/get-session` request returns null while the other device is unaffected. **Deliberately not built, flagged rather than silently skipped:** (1) the account-lockout audit entry TECHNICAL-PLAN §8.2 asks for — `audit_events.workspace_id` is `not null` and a failed sign-in has no workspace yet to attach to (the email may belong to zero, one or several), so this inherits the P8-T03 open question ("decide here whether instance-level audit gets its own chain") rather than forcing a workspace-scoped row onto a workspace-less event or fanning one event out across every workspace the address happens to touch. (2) Rate limiting through the Cache port on invitations, channels and exports: authentication already has its own limiter (Better Auth's built-in one, in-memory, per address — a separate mechanism from the Cache port's `rateLimit`, not touched here); channels and exports have no built endpoint yet to rate limit; and invitation acceptance has no `apps/web` route calling it yet either (P2-T04 built the action, not the screen), so there is no real caller to thread a Cache instance and an address through to — building that wiring now would be scaffolding with nothing to prove it against. Per-member limits on "the API" broadly are named in TECHNICAL-PLAN §8.2 with no list of which actions or what numbers, which is a scope decision for a human, not one to invent. (3) The encryption root key was checked against the placeholder-secret pattern `BETTER_AUTH_SECRET` already has (`packages/config/src/env.ts`'s `PLACEHOLDER_SECRETS`/`assertProductionSecrets`) and found not to need it the same way: the root key is not in the `Env` schema at all today, generated instead by `./openokr up` on first run per its own documented behaviour, so there is no in-app default for a placeholder check to guard against — extending the schema to hold it would be inventing a second provisioning path for a decision this task was not asked to make. **Verified** under Node 22: typecheck (`packages/core`, `packages/adapters`, `apps/web`), Biome, the boundary gate, the migration linter and the soft-delete lint all pass; `packages/config`'s own suite re-ran clean. **Not verified:** `packages/core/test/{freeze,auth}.test.ts` and `packages/adapters/test/drivers.test.ts` — no Docker/Postgres in this sandbox, same gap as every prior Phase 2 row. This row is worth the *whole* suite before merging, not just its own new files: the freeze check runs ahead of every write in `runOperation`, on every existing test in every prior task's file, and reasoned through rather than run here — every prior test's workspace stays `active` throughout its own test, so the new check is a no-op for all of them, but "reasoned through" is not "proven" without `pnpm db:up && pnpm test` |
| P2-T10 | App shell + design system | in_review | task/p2-t10-app-shell-design-system | 2026-08-10 | Built from zero: no Tailwind, no component infrastructure, no `packages/ui` implementation existed before this. **Dependencies approved by the human** (Base UI + Motion/SmoothUI, generically) resolved to the concrete packages the locked stack names: `tailwindcss`/`@tailwindcss/postcss` (v4, CSS-first — no `tailwind.config.js`), `@base-ui-components/react` (1.0.0-rc.0), `motion`, `lucide-react`, plus `clsx`/`tailwind-merge`/`class-variance-authority` (the shadcn/ui variant-and-class-merge trio every shadcn component is written against, not a separate dependency decision) and `@tanstack/react-query` with its persist-client and sync-storage-persister add-ons (item 9, and already named in CLAUDE.md's locked stack). Also added, inferred rather than asked separately: `jsdom`/`@testing-library/react`/`@testing-library/user-event`, dev-only, because this task's own required "keyboard and focus component tests" are unwritable without a DOM testing seam. **Design tokens** (`packages/ui/src/styles/tokens.css`): the mockups' own `style.css` values (§10's "proposed default") re-expressed as Tailwind v4 `@theme inline` mappings over runtime CSS custom properties, so `[data-theme="dark"]`/`[data-density="compact"]` repaint every utility class without a `dark:` variant on each one (a `@custom-variant dark` retargets Tailwind's own variant to the attribute instead of `.dark`). One deliberate spec-vs-mockup conflict, per §10's own tie-break rule: §2 names 14 as the base text size, the mockup's own body is 13px; this file uses 14, and the mockup source is *not* edited to match, because doing so without re-running its PNG render step would leave the source and its screenshots mismatched — recorded here as the follow-up §10 allows instead. **Dark mode**: an inline `<head>` script (`theme-script.ts`, Next's own documented no-flash pattern) sets concrete `data-theme`/`data-density` before hydration from `localStorage`, resolving "system" via `matchMedia` at that moment; `ThemeProvider`/`useTheme` (`theme-provider.tsx`) take over afterwards, re-applying on every mount to survive React Strict Mode's dev-only double-mount per Next's own documented caveat. Persisted to `localStorage` only, not per-member: TECHNICAL-PLAN §4.14 does name a member `theme`/`density` setting, but nothing writes those columns yet (P2-T03 built only timezone/avatar/bio on `updateOwnProfile`), and wiring a real per-member write is this task's own scope boundary, not this task's to build — flagged as a follow-up for whichever task adds those columns. **Core components** (`packages/ui/src/components/`): `Button`/`Card`/`Chip`/`Bar`/`VerdictDot`/`Avatar`/`Kbd`, each reimplementing a mockup class (`.btn`/`.card`/`.chip`/`.bar`/`.vd`/`.av`/`.kbd`) as Tailwind utilities against the tokens rather than copied CSS, with a preview page at `apps/web/app/dev/components` (`notFound()` outside development — a design system's own showcase is not something a production instance serves). Not built: the ~50 other §5 inventory names (`WorkMapTree`, `KpiGrid`, `SessionStage`, …) — each is entity-specific to a screen that does not exist yet (Phase 3/4), and building one now would be throwaway. **Shell** (`packages/ui/src/shell/`): `Sidebar`/`Topbar`/`TopbarSearch`/`CycleStrip`/`MobileTabBar`/`AppShell` are pure presentational slots; `apps/web/lib/app-shell.tsx` (`AppShellLayout`) is the one place that wires them to real data — `navigationFor("sidebar"|"admin", level)` for nav items (today: Overview, Security, and a single "Admin" link; most of §3's target IA — Review, Inbox, Cycle, Goals, KPIs, Work, Spaces — renders empty because those modules do not exist yet, which is the honest state, not a dead-link placeholder). The workspace switcher (P1-T06 follow-up) now always shows a brandmark; the actual `<select>`-and-submit switching control stays conditional on 2+ memberships as before, and its `onChange` auto-submit is a separate tiny client component (`workspace-select.tsx`) because a Server Component's own JSX cannot carry an event handler. **Responsive** (§3): hidden below Tailwind's own `md` (768px) / icon-rail from `md` to below `xl` (1280px) / full from `xl` — exactly §3's own breakpoints, needing no custom Tailwind config, just the right utility on each element; the mobile tab bar takes over below `md`. **Keyboard registry** (`packages/ui/src/keyboard/`): `KeyboardRegistryProvider`/`useKeyboardShortcut`/`ShortcutOverlay` (a Base UI `Dialog`, opened by `?`). Deliberately not wired to `⌘K`/`⌘J`/`c`/`j`/`k`/`x`/`e`/`⌘⏎`/`[`/`]` — each names a screen (the palette S-32, Ask AI S-39, list/detail views) that does not exist yet, and a key bound to nothing would be a dead button, the same thing §4's "AI degradation" rule refuses for a missing provider, generalised to a missing feature. **A real bug found and fixed while writing this task's own tests, not a pre-existing one**: `useKeyboardShortcut`'s effect depended on the caller's `shortcut` object by identity, and every real caller (including `ShortcutOverlay`'s own `?` registration) constructs a fresh literal every render — an identity-dependent effect plus a registry `register()` that unconditionally replaced state meant every render re-registered, which re-rendered the provider, which re-rendered every consumer, forever. Fixed on both sides: the hook now depends on the shortcut's primitive fields, not its identity, and `register()` skips the state update when the incoming entry is equal by value to what is already registered, so a future caller that also reconstructs its shortcut literal every render cannot reintroduce the loop either. Caught because the test suite hung/crashed the Vitest worker outright rather than failing normally — genuinely alarming until traced, and worth naming here because it would have shipped into the running app exactly as built otherwise. **Message catalogue** (`packages/ui/src/i18n/`): `en.json`/`ms.json` plus `toPseudoLocale`/`findUnwrappedText`, the two-sided pseudo-locale check (§8) — a value transformed to be visibly non-English and reliably longer, wrapped in brackets, so any rendered text sitting *outside* a wrapper under the pseudo locale is provably a hardcoded string, proven directly in `catalogue.test.tsx` against both a real catalogued component and a deliberately unlocalized one. Scope today: only the strings this task's own shell components render (`ShortcutOverlay`, `TopbarSearch`) — §8 wants every user-facing string catalogued, and there is barely any UI yet for that to mean more until later phases build screens with real text, each adding its own keys the same way. Bahasa Melayu values are a good-faith stub (the task card's own wording) for these simple, common words, not a reviewed translation. **Persisted client cache + stale-tab reload** (item 9): `QueryProvider` persists TanStack Query's cache to `localStorage` with `buster: APP_BUILD_ID` (the library's own documented "discard the persisted cache when this string changes" mechanism); `APP_BUILD_ID` is a new `packages/config` env var (default: a random id generated once per process start, so a restart alone counts as a new "deployment" even with the var unset — a real deployment sets it to a git SHA). `useStaleDeployment`/`StaleDeploymentWatcher` poll a new authenticated endpoint, `/api/app-version` — deliberately **not** added to the existing public `/api/health`, which already carries an explicit P1-T09 decision that a version string is reconnaissance for an unauthenticated caller; this endpoint carries the same value; behind a session instead of relaxing that. The reload-once guard is structural, not a flag that could be forgotten: `shouldReload` refuses to fire again for a build id it has already reloaded for, recorded in `sessionStorage`, proven with a mocked repeat check in `use-stale-deployment.test.tsx`. **Auth screens (S-35) restyled**, and the progressive-enhancement decision the task card asked for is answered directly: JavaScript stays required. Passkey/WebAuthn is one of the offered methods and has no server-only fallback at all — the ceremony is `navigator.credentials`, unavailable without JavaScript on any browser — so a parallel no-JS path for password-only sign-in would be two flows to keep in sync for a case (a modern browser with JavaScript disabled) far rarer than "no WebAuthn support," which the JS-required path already handles by falling back to a password. `AuthCard`/`Field`/`FormError` restyled in place; every screen's logic is untouched. Not built: workspace branding on the card and the pre-authentication language switcher §3 also asks for — no branding colour reaches an unauthenticated request yet, and the switcher needs a resolved instance/workspace default this route does not look up today; both flagged rather than invented. **Proving dashboard restyled**: `workspace-overview.tsx`/`overview-skeleton.tsx`/`error.tsx`/`not-found.tsx` now render inside/alongside the shell with `Card`/`Button`; `error.tsx`/`not-found.tsx` stay standalone (no shell assumed), since whatever produced either state may have been the shell's own data fetch. **Admin shell restyled** (`admin/layout.tsx`): the P2-T08 comment "unstyled on purpose: the design system arrives at P2-T10" is closed here — it now wraps with `AppShellLayout` and a styled section nav, kept deliberately separate from the primary sidebar's own single "Admin" link, matching §3's diagram (no expanded submenu there). `apps/web`'s locale is hardcoded to `"en"` for now (`TranslationsProvider locale="en"` in the root layout): resolving a real member/workspace/instance language into it needs a session-aware read the root layout cannot do for pages that are also reachable signed out, and is a follow-up once that resolution has a natural home. **Verified** under Node 22: typecheck (`packages/ui`, `apps/web`, `packages/config`), Biome (including a new `css.parser.tailwindDirectives: true` in `biome.json`, needed for `@theme`/`@custom-variant`/`@source` — and a real, separate CSS-comment bug caught along the way: a token file comment containing a literal `--a-*/--b-*` sequence closed its own `/* */` block early, corrupting everything after it, fixed by rewording), the boundary gate (215 files) and the migration/soft-delete lints (unaffected, re-run anyway) all pass. `packages/config`'s suite re-ran clean (78 tests) and `apps/web`'s own suite re-ran clean (16 tests, unaffected by this task but re-checked since `app/page.tsx`/`app/layout.tsx` changed significantly). **`packages/ui`'s own suite actually ran here** — jsdom, no database dependency, a first for a UI-bearing package this session — 41 tests across 7 files: theme/density persistence end to end, the keyboard registry and its shortcut-matching logic (including the regression test for the render-loop bug above), the pseudo-locale check catching a real hardcoded string, the stale-deployment reload-once guard, responsive class assertions on the sidebar/tab-bar, and core component rendering. Licence gate verified manually the same way as every prior task in this sandbox (`pnpm licenses list --json`, diffed against the script's own allow-list by hand, since its `pnpm` subprocess spawn does not run here): every new dependency is MIT, Apache-2.0 or ISC, all already allowed. **Not verified, flagged rather than silently skipped:** (1) `ShortcutOverlay`'s and `AvatarMenu`'s actual appearance and the responsive breakpoints' actual behaviour in a real browser — this sandbox has no Docker/Postgres to run the dev server against, and `pnpm test:e2e`'s Playwright suite needs a build; the responsive tests here only assert the right Tailwind classes are present, not that a resize does what they claim. (2) The workspace's own brand colour and the S-35 pre-authentication language switcher, named above. (3) The dependency-inference for test tooling (`jsdom`/testing-library) — reasonable given the task's own required tests, but not a separate ask the way the UI framework choices were. P1-T08 remains `in_review`, not `done`, on the row's own formal dependency; already flagged (not re-flagged) in the cross-phase gap review above, which is where this was surfaced and left for a human rather than self-promoted |
| P2-T11 | Rich text editor | in_review | task/p2-t11-rich-text-editor | 2026-08-10 | **A design document first, per the task's own card**: `docs/design/rich-text-editor.md` pins down what TECHNICAL-PLAN only named in the abstract ("editor JSON... a version integer... one shared core module") — the stored shape is TipTap's own document JSON directly, never a custom translation layer, restricted to an explicit node/mark allow-list this document defines from scratch (nothing prior specifies one). **`packages/core/src/rich-text/`** (`schema.ts`/`validate.ts`/`render.ts`/`excerpt.ts`/`extract.ts`), pure, no DB/network/DOM, exported from the package root: `parseRichText` (two-pass validation — attribute shape via Zod, then nesting via an explicit `NESTING_RULES` lookup, so a `tableRow` cannot sit directly inside a `paragraph` even though both are individually allow-listed types), `renderRichTextToHtml` (a fixed node-type-to-tag table, resolver callbacks for mention/entityLink/attachment that degrade to plain text with no resolver or a failed lookup, never a broken link), `excerptRichText`, and the decode-safe `extractMentionIds`/`extractAttachments` (TECHNICAL-PLAN §4.10's own line: malformed input returns `[]`, never throws). **The one real integration point today**: `people.updateOwnProfile`'s `bio` field, upgraded from P2-T03's own placeholder (`z.unknown()`) to a `.refine()` against `isValidRichText` — P2-T03's STATUS.md row named this exact module as what it was waiting on. No other rich-text-bearing table exists yet (comments/documents/goal descriptions are later phases), so every other consumer named in TECHNICAL-PLAN §1 (mail, exports, the inbox, the feed) has nothing to wire into yet — the functions exist and are tested against synthetic documents, not against a real caller. **A real architecture-boundary catch while writing the design doc, before any editor code**: TECHNICAL-PLAN §1's own package table restricts `packages/ui` to depending on `packages/method` only, never `packages/core` — where the validator lives. The design doc's first draft assumed the editor component would call `parseRichText` directly; caught and fixed before implementation by taking `validate` as an optional prop instead (the same dependency-injection shape already used for the member/entity search functions and the upload function, since `packages/ui` has no reach into either P2-T05's blob pipeline or a member directory). **`packages/ui/src/rich-text/`**: `RichTextEditor` (`@tiptap/react`), configured to exactly the design doc's allow-list — `@tiptap/starter-kit` with `dropcursor`/`gapcursor`/`undoRedo`/`underline`/`listKeymap` explicitly disabled (all bundled by default in the installed v3, none on the allow-list) and `heading`/`link` configured down to levels 1-3 and `http`/`https`/`mailto` only; `@tiptap/extension-table`; two separately-configured `@tiptap/extension-mention` instances (`@` for members producing `mention` nodes, `#` for entity autolink via `Mention.extend({name: "entityLink"})` producing a distinct node type with `shortId`/`label` attrs — one Mention instance cannot produce two node types even with multiple trigger characters, confirmed by reading the installed extension's own types before assuming otherwise); a hand-written `attachment` `Node.create()` with a React node view (upload spinner / filename+remove, never a link inside the editor itself); and a `/` slash-menu built directly on `@tiptap/suggestion` since it runs editor commands rather than inserting a node. A shared `SuggestionList` component and `createSuggestionRender` factory back all three popups (mention, entity link, slash) with one keyboard-navigation implementation. **Upload flow** (§7): an `attachment` node appears immediately with `status: "uploading"`, flips to `"ready"` with a real `blobId` once the caller's own `uploadFile` prop resolves, or is removed outright on failure — never left pointing at nothing. Submit gating (`hasUploadsInProgress()` on the component's imperative handle) is the literal acceptance line: a submit while an upload is in flight must wait or fail loudly, never drop the attachment silently — enforced here client-side, with the server-side write boundary (`bioInputSchema`'s `isValidRichText` call) as the actual backstop, the same "client warns, server enforces" split as the `validate` prop. **Draft autosave** (§8): `draft.ts` (pure: `fingerprint` — a cheap rolling hash, not a cryptographic digest, matching P2-T10's `avatarToneFor` reasoning — plus `createDraft`/`isDraftUsable`) and `use-draft-autosave.ts` (the `localStorage` wiring). "A draft against changed base content does not resurrect" is `isDraftUsable`'s own fingerprint comparison, not a merge or a warning; expiry (14 days) is checked identically. A dev-only preview page at `apps/web/app/dev/rich-text` (matching P2-T10's `/dev/components` precedent) exercises the whole thing end to end with a mocked member/entity search and a mocked (artificially delayed) upload, wiring the real `isValidRichText` in as the `validate` prop. **New dependencies**: `@tiptap/core`/`pm`/`react`/`starter-kit`/`extension-table`/`extension-mention`/`suggestion`, all pinned to the identical `3.29.2` (every `@tiptap/*` v3 package peer-pins its siblings to an exact version, not a range, confirmed by reading the published peerDependencies rather than assumed) — TipTap itself was already named and approved alongside P2-T10's UI dependencies; picking the exact packages and pins is this task's own implementation decision, the same status as P2-T10's Base UI package resolution. **Verified** under Node 22: typecheck (`packages/core`, `packages/ui`, `apps/web`), Biome, and the boundary gate (229 files) all pass; `packages/config`/`apps/web` suites re-ran clean. `packages/ui`'s own suite ran here (jsdom, no DB) with the new files included: 54 tests total, 13 of them new (draft fingerprint/expiry, the hook's localStorage wiring including the draft-resurrection case, and — genuinely uncertain going in — the TipTap editor mounting cleanly in jsdom with no crash, proven directly rather than assumed). `packages/core`'s own rich-text suite (29 tests: the golden round-trip fixtures, the malicious `javascript:` link and unknown-node/mark rejections, decode-safe extraction on malformed input) was run against a throwaway, uncommitted Vitest config with no `globalSetup` — the real committed `packages/core/vitest.config.ts` still opens a database for the whole package as every prior row notes, but this module is genuinely pure, so this is real, disclosed execution proof rather than "written, never run." The 5 new schema-level tests added to `packages/core/test/people.test.ts` (`bio`'s Zod input schema accepting/rejecting documents directly) could not be run the same way — that file's top-level `beforeEach` provisions a real workspace regardless of which test is selected, so it inherits the whole file's existing Docker/Postgres gap. Licence gate verified manually (`pnpm licenses list --json` diffed against the allow-list by hand, same method as every prior task): every new package and its `@floating-ui/*`/`linkifyjs` transitives are MIT, already allowed. Found and fixed one unrelated pre-existing defect while touching `people.test.ts` for the first time this session: two genuinely dead imports (`activities`, `eq`) Biome caught once run against this file — removed. **Not verified, flagged rather than silently skipped:** (1) real-browser behaviour (drag-drop upload, the floating suggestion popups' actual position, `?` overlay-style focus trapping inside the editor) — this sandbox has no Docker to run the dev server or Playwright against; the jsdom mount proves the extension set assembles and renders without crashing, not that positioning or drag-and-drop work in a real browser. (2) Every consumer TECHNICAL-PLAN §1 names beyond `bio` (mail, exports, comments, documents) — none exist yet, so `renderRichTextToHtml`/`excerptRichText`/the extraction functions are proven against synthetic fixtures only. (3) `codeBlock`'s allow-list deliberately excludes syntax highlighting — not asked for by S-30, flagged as a natural future addition rather than built speculatively |
| P2-T12 | Data-change runner | in_review | task/p2-t12-data-change-runner | 2026-08-10 | `packages/db/src/data-change.ts`: `runDataChanges` against a plain client, mirroring `migrate.ts`'s own shape (a `_data_changes` ledger table it creates itself, an advisory lock distinct from the migration runner's so the two never contend, name-ordered scripts) but batched and resumable rather than one statement per script, because a backfill can be too large for one transaction the way a schema change never is. A script declares `expects`: the columns and `information_schema.columns.data_type` values its SQL depends on, checked before every run, not once at review time — this is what "a later schema change cannot silently alter what an old script does" (the task's own acceptance line) means as code rather than a comment. The ledger records a cursor and commits after every batch, so a crash between batches loses no progress; a completed script is skipped on every later run, which is the acceptance criterion itself ("run twice... does nothing... the ledger shows one completion"), proven in a test that forces a mid-run crash and confirms the resume neither re-runs the already-committed batch nor restarts from zero. One design choice worth naming: a migration's checksum covers its literal SQL text, but a script here is TypeScript code, not a static string, so its checksum instead covers `{name, expects}` — the declared, frozen contract — and changing that contract on an in-progress (started, not completed) script is refused rather than silently accepted, matching the spirit of "immutable once it has run" without needing to hash a function body. The one sample change (`data-changes/0001_backfill_member_timezone.ts`) sets a member's `timezone` from their workspace's own settings wherever it is still null, batched by a keyset on `id` (this schema's ids are time-ordered, so ascending order is a cheap, stable resume point) — flagged plainly in its own header as a worked example rather than a real gap this codebase has today, since every member the Operation pipeline provisions already gets a timezone at insert time; the importer (P6-T01/T02) is the real future case it is written for. `pnpm db:change` (`packages/db/src/bin/data-change.ts`, wired into the root `package.json`) is the new command CLAUDE.md's own "commands that do not exist yet" table named for this task; moved it into the main command list and removed the stale row. The conventions document is `docs/design/data-change-conventions.md`. **A stray finding, not chased further because it is outside this task's scope:** while editing that same CLAUDE.md table, `pnpm gen:contract`'s row cites "P2-T09" as its arrival task, but P2-T09's actual IMPLEMENTATION-PLAN deliverables (security baseline: rate limiting, lockout, CSP, sessions, the freeze overlay, placeholder secrets) say nothing about contract generation, and `packages/core/src/actions/registry.ts`'s own header comment names "P5-T07 onwards" for exactly this generated-projections work instead — the same shape of citation defect already known and flagged for P2-T16 (`AI-NATIVE-PLAN.md §1.7`). Left as found, for a human to correct the same way. **Verified** under Node 22: typecheck (`packages/db`), Biome, the boundary gate, the migration linter and the soft-delete lint all pass; `packages/config`'s own suite re-ran clean. **Not verified:** `packages/db/test/data-change.test.ts` — no Docker/Postgres in this sandbox, same gap as every prior Phase 2 row; this one is unusually self-contained (a scratch database per test, like `migrate.test.ts` already does), so `pnpm db:up && pnpm --filter @openokr/db test` alone would be enough to close it, without needing the wider suite the way the operation.ts-touching rows do |
| P2-T13 | AIProvider port + drivers | in_review | task/p2-t13-ai-provider-drivers | 2026-08-10 | The inherited port-lifecycle follow-up was already closed in the cross-phase gap review just above this table, ahead of this task needing it — every port including `AIProvider` already declared `stop()` before the first real driver was written. The port itself (`chat`/`stream`/`chatWithTools`/`embed`/`extract`/`capabilities`) was already built at P1-T04 with only the `off` driver behind it; this task adds the six real ones AI-NATIVE-PLAN §3.2 names. Human approved three new dependencies after being asked precisely which ones, not just "AI SDKs generally": `@anthropic-ai/sdk` (native, for prompt caching and because its Messages API is not OpenAI-shaped), `openai` (drives four of the six drivers — `openai`, `openrouter`, `ollama` and the fully generic `openai-compatible` all speak the identical chat-completions API, just a different base URL and key, so one client and one implementation, `OpenAiCompatibleProvider`, covers all four as thin presets or direct use), and `@google/genai` (native, for Gemini's own tool-calling and structured-output fidelity). Two packages needed an explicit `allowBuilds` decision under pnpm's script-gating (`@google/genai` itself and its transitive `protobufjs`); checked what each package's flagged lifecycle script actually does before approving both — neither runs anything against a published install. Exact SDK call shapes (constructor options, request/response field names, streaming event shapes) were verified by reading the installed `.d.ts` files directly rather than trusted from training data, since all three packages are recent enough that memorised shapes risked being stale; this caught real differences from what would have been assumed, most importantly: OpenAI's `max_tokens` is deprecated in favour of `max_completion_tokens`; Anthropic's `MessageParam.role` now type-checks a `'system'` value for an unrelated beta feature (mid-conversation system blocks) that must not be confused with a general system message — the top-level `system` parameter is still the only real one; Google's client has no `fetch` injection point at all, only `httpOptions.baseUrl`, so `GoogleProviderOptions.client` exists as this driver's own seam for a pre-built stand-in instead; and Gemini's own embedding response carries no token-usage field of any kind. Every driver ships its own `..._DEFAULT_TIER_MODELS` seed (AI-NATIVE-PLAN §3.4: "a driver added without a default tier map is incomplete"), read through `defaultTierModelsFor` — free-text seeds, not a catalogue, meant to be overridden the moment P2-T15 exists. `createAIProvider` (`packages/adapters/src/create-ai-provider.ts`) is the one exported entry point; every driver class itself stays private to this package, matching how `createMailer` already works and keeping "adding a provider is a new driver behind the same port, never a change to feature code" true of the export surface, not just the port's own shape. Not wired into `createAdapters()`: which provider and key apply is resolved by configuration that changes without a restart, which is P2-T14's precedence resolver (user key, then workspace, then deployment, then off), not something decided once at process start. The driver contract is written up at `docs/design/ai-provider-driver-contract.md`. **Verified** under Node 22: typecheck and Biome on every new file, the boundary gate (185 files). The licence gate's own wrapper script could not run in this sandbox (`spawn pnpm ENOENT` — the same PATH quirk documented all session, not a new one), so it was run manually instead: `pnpm licenses list --json --prod` against the full installed tree, diffed by hand against `scripts/check-licences.ts`'s own allow list — every licence across all three new packages and their transitive dependencies (including `protobufjs`, BSD-3-Clause) is already on it. **Not verified:** `packages/adapters/test/ai-{openai-compatible,anthropic,google,mock}.test.ts` and `create-ai-provider.test.ts` — no Docker/Postgres in this sandbox, same gap as every prior Phase 2 row touching this package; the streaming tests for the OpenAI-shaped and Anthropic drivers carry the most risk of the new files, since they depend on a hand-authored SSE fixture matching each vendor's real event framing exactly and could not be run-and-corrected here the way the non-streaming ones' simpler JSON fixtures could be reasoned through with higher confidence — read those two `describe("stream", ...)` blocks first when running the suite |
| P2-T14 | AI configuration, keys, encryption and rotation | todo |  |  |  |
| P2-T15 | Model catalogue, tier routing, structured output and prompts | todo |  |  |  |
| P2-T16 | Usage metering, quotas and hard caps | todo |  |  | The blocking doc defect is closed: IMPLEMENTATION-PLAN.md's citation was `AI-NATIVE-PLAN.md §1.7`, which never existed (§1 has no subsections), corrected 2026-08-10 to `§4` ("Budgets and limits") with the human's explicit direction to fix it directly rather than hold the task open. Still depends on P2-T14, not yet started |
| P2-T17 | Agent runtime: agents, runs, sandbox, proposals | todo |  |  | Carries a P1 hardening follow-up: PgBossJobQueue.stop() clears #running but not #started, so a start() after a stop() skips createQueue for every queue it has already seen and then sends into queues pg-boss no longer knows about. This is the first component likely to cycle the queue inside one process |

## Phase 3: The OKR core

| Task | Title | Status | Branch / PR | Updated | Notes |
|---|---|---|---|---|---|
| P3-T00 | OKR core design gate | todo |  |  | Needs an explicit design approval. Golden-master matrices reviewed line by line |
| P3-T01 | Spaces | todo |  |  | Carries a P1-T09 follow-up: the first-run wizard is specified to offer demo data and does not, because demo data means objectives, key results and a cycle, and none of those exist before Phase 3. Add the offer here, with a pnpm db:seed that has something to seed |
| P3-T02 | Annual frame, cycles and rhythm settings | todo |  |  |  |
| P3-T03 | The guided cycle workflow | todo |  |  |  |
| P3-T04 | Goals + key results | todo |  |  |  |
| P3-T05 | Scoring and health engine | todo |  |  |  |
| P3-T06 | Cadence engine + staleness | todo |  |  |  |
| P3-T07 | Check-ins: snapshots, publication, acknowledgement, voting | todo |  |  |  |
| P3-T08 | Review inbox | todo |  |  |  |
| P3-T09 | Alignment: parents, dependencies, the alignment engine | todo |  |  |  |
| P3-T10 | Goal surfaces: explorer, detail, alignment studio | todo |  |  |  |
| P3-T11 | Work Map | todo |  |  | Carries a P1-T08 follow-up: the proving dashboard at / is scaffolding, deliberately thin and unstyled. S-01 is the real home and replaces this route entirely here |
| P3-T12 | KPIs: categories, records, grid | todo |  |  |  |
| P3-T13 | KPI formula engine | todo |  |  |  |
| P3-T14 | KPI trees, corridors, recovery OKRs | todo |  |  |  |
| P3-T15 | Scorecard, cycle archive and feed-forward | todo |  |  | Scorecard points layer stays off unless the human funds it |
| P3-T16 | Comments, reactions and discussion wiring | todo |  |  |  |
| P3-T17 | Demo workspace builder + seed | todo |  |  |  |

## Phase 4: The coaching layer

| Task | Title | Status | Branch / PR | Updated | Notes |
|---|---|---|---|---|---|
| P4-T00 | Coaching design gate | todo |  |  | Needs an explicit design approval. Rule corpus and trigger catalogue reviewed line by line |
| P4-T01 | The method package | todo |  |  |  |
| P4-T02 | The quality engine and Draft Coach surfaces | todo |  |  |  |
| P4-T03 | Publish gates | todo |  |  |  |
| P4-T04 | The nudge engine, triggers and escalation | todo |  |  |  |
| P4-T05 | The OKR Champion agent | todo |  |  |  |
| P4-T06 | The OKR Coach agent | todo |  |  |  |
| P4-T07 | Weekly session: confidence round, voting, blockers | todo |  |  |  |
| P4-T08 | Weekly session: commitments, digest, streaks | todo |  |  |  |
| P4-T09 | Monthly review and decision log | todo |  |  |  |
| P4-T10 | Quarterly review: session shell, scoring, narratives | todo |  |  |  |
| P4-T11 | Quarterly review: retro, diagnostic, reset | todo |  |  |  |
| P4-T12 | Minutes, exports and review feed-forward | todo |  |  |  |
| P4-T13 | Embeddings and retrieval | todo |  |  |  |
| P4-T14 | Copilot | todo |  |  |  |
| P4-T15 | Coaching and rhythm assists | todo |  |  |  |

## Phase 5: Reach: channels, agents, work

| Task | Title | Status | Branch / PR | Updated | Notes |
|---|---|---|---|---|---|
| P5-T00 | Reach design gate | todo |  |  | Needs an explicit design approval |
| P5-T01 | Channel port, email driver and routing | todo |  |  |  |
| P5-T02 | Slack driver | todo |  |  |  |
| P5-T03 | Microsoft Teams driver | todo |  |  |  |
| P5-T04 | WhatsApp driver | todo |  |  |  |
| P5-T05 | Telegram driver | todo |  |  |  |
| P5-T06 | The chat command surface | todo |  |  |  |
| P5-T07 | Public contract projections: REST, OpenAPI and the command line | todo |  |  |  |
| P5-T08 | MCP authorisation server | todo |  |  |  |
| P5-T09 | MCP transport, sessions and tool catalogue | todo |  |  |  |
| P5-T10 | Initiatives | todo |  |  |  |
| P5-T11 | Tasks and the OKR board | todo |  |  |  |
| P5-T12 | Documents and attachments | todo |  |  |  |
| P5-T13 | Search, palette and exports | todo |  |  |  |

## Phase 6: Data: import, export, portability

| Task | Title | Status | Branch / PR | Updated | Notes |
|---|---|---|---|---|---|
| P6-T01 | CSV and XLSX importer with the AI mapper | todo |  |  |  |
| P6-T02 | FlowyTeam connector | todo |  |  |  |
| P6-T03 | FlowyTeam strategy mappers | todo |  |  |  |
| P6-T04 | FlowyTeam work and collaboration mappers | todo |  |  |  |
| P6-T05 | Workspace export and import | todo |  |  |  |
| P6-T06 | Backups and restore drills | todo |  |  |  |
| P6-T07 | Migration cutover rehearsal | todo |  |  |  |

## Phase 7: Hardening

| Task | Title | Status | Branch / PR | Updated | Notes |
|---|---|---|---|---|---|
| P7-T01 | Performance budgets and indexing at scale | todo |  |  | Carries a P1-T07 follow-up: the audit chain takes a per-workspace advisory lock, so concurrent writes in one workspace serialise at the end of their transaction. Measure it. The fallback, if it binds, is a sequence column with retry, which keeps verifiability |
| P7-T02 | Load and soak testing | todo |  |  |  |
| P7-T03 | Security review, supply chain and tenant fuzzing | todo |  |  |  |
| P7-T04 | Agent, nudge and channel safety hardening | todo |  |  |  |
| P7-T05 | Accessibility audit and web vitals | todo |  |  |  |
| P7-T06 | Observability | todo |  |  |  |
| P7-T07 | Method conformance audit | todo |  |  |  |
| P7-T08 | Privacy: export, erasure and retention | todo |  |  |  |

## Phase 8: Cloud, enterprise and launch

| Task | Title | Status | Branch / PR | Updated | Notes |
|---|---|---|---|---|---|
| P8-T01 | Cloud design gate | todo |  |  | Needs an explicit design approval |
| P8-T02 | Tenant provisioning, signup and onboarding | todo |  |  |  |
| P8-T03 | Operator console | todo |  |  | Carries a P1-T09 follow-up: instance settings writes sit outside the Operation pipeline behind an openokr:allow-mutation marker, because the pipeline needs a workspace and an acting member for its activity and audit rows and audit_events.workspace_id is not null, so an instance write has no chain to join. Decide here whether instance-level audit gets its own chain |
| P8-T04 | Transparent support access | todo |  |  |  |
| P8-T05 | Plans, seats and limits | todo |  |  | Plans and seats stay behind a flag that is off for self-host |
| P8-T06 | Cloud operations | todo |  |  |  |
| P8-T07 | Single sign-on | todo |  |  |  |
| P8-T08 | Directory sync and provisioning | todo |  |  |  |
| P8-T09 | Multi-factor policy | todo |  |  |  |
| P8-T10 | Audit export, chain verification and the air-gap guide | todo |  |  |  |
| P8-T11 | Documentation site | todo |  |  |  |
| P8-T12 | Template gallery and rhythm guides | todo |  |  |  |
| P8-T13 | Hosted demo instance | todo |  |  |  |
| P8-T14 | Launch | todo |  |  |  |
