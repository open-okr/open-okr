// The workflow of each module, for the Module Guide sheet, the summary line
// on each module band, and the diagram on the Read Me sheet.
//
// `phase` puts a module in a column of the diagram. `needs` lists the modules
// that must be done first. `gives` is what the module leaves behind for the
// ones after it. Keep one entry per module in MODULES (uat-cases.mjs).

export const PHASES = [
  ["setup", "1. Set up"],
  ["team", "2. Build the team"],
  ["plan", "3. Plan the cycle"],
  ["run", "4. Run the cycle"],
  ["close", "5. Close and check"],
  ["connect", "6. Integrations"],
];

export const GUIDE = {
  M01: {
    phase: "setup",
    purpose: "Turn an empty server into an instance with one owner",
    screens: "/setup, /setup/account, /sign-up",
    who: "Admin",
    flow: ["Open the instance and get sent to setup", "Name the instance Northwind Labs", "Create the owner account", "Check that registration is now closed"],
    needs: [],
    gives: "An owner account. Everybody after this joins by invitation",
    setup: "A fresh instance with an empty database",
    minutes: 10,
  },
  M02: {
    phase: "setup",
    purpose: "Give the new workspace its name, clock, rhythm and starting content",
    screens: "/welcome",
    who: "Admin",
    flow: ["Name the workspace and pick its timezone", "Choose the check-in rhythm", "Skip the invite", "Apply the OKR starter cycle template", "Land on the work map", "Operator runs deploy/staging/seed.sh for the seven personas", "One persona signs in"],
    needs: ["M01"],
    gives: "A current cycle, one objective with 3 key results, a KPI, a KPI tree, the weekly session M15 needs, and seven persona accounts",
    setup: "Someone with shell access to the staging host for the seed step",
    minutes: 20,
  },
  M03: {
    phase: "setup",
    purpose: "Check that workspace settings save, reload and reset",
    screens: "Admin: General, Branding, Rhythm and thresholds",
    who: "Admin",
    flow: ["Change timezone and language", "Check the mail warning", "Set a brand colour", "Set the check-in rhythm", "Rename a concept, then set it back"],
    needs: ["M02"],
    gives: "The rhythm every later reminder and due date follows",
    setup: "",
    minutes: 15,
  },
  M04: {
    phase: "team",
    purpose: "Check the invitation path with spare people, since the personas were seeded",
    screens: "Admin, Invitations; /join/<token>; /sign-up",
    who: "Admin, then spare addresses",
    flow: ["Admin invites +leaver personally", "Test Leaver opens the link and signs up", "A wrong address is refused", "A shared link admits +guest1 and +guest2", "A domain outside the list is refused", "Revoked and made-up links are refused"],
    needs: ["M02"],
    gives: "Test Leaver, the spare account M19 erases",
    setup: "The shared inbox and a private browser window",
    minutes: 15,
  },
  M05: {
    phase: "team",
    purpose: "Prove sign-in, password reset and the second factor protect an account",
    screens: "/sign-in, /forgot-password, /reset-password, /backup-code, Account, Security",
    who: "Jonas, Mei, Amara, Sara, Priya",
    flow: ["Refuse a wrong password", "Reset a forgotten password by mail", "Turn on one-time codes for Amara", "Sign in with a code and with a backup code", "Add a passkey", "Revoke another device"],
    needs: ["M02"],
    gives: "Confidence that accounts are safe before real data goes in",
    setup: "Mailpit or SMTP for the reset mail. An authenticator app",
    minutes: 30,
  },
  M06: {
    phase: "team",
    purpose: "Give everyone a title and a manager so the org chart is real",
    screens: "People, People / <person>",
    who: "Admin, Mei, Jonas, Tomas",
    flow: ["Admin sets title and manager for each persona", "Check the org chart", "A loop in the chain is refused", "Members edit only their own profile", "Suspend and restore Tomas"],
    needs: ["M02"],
    gives: "Reporting lines, used by the org chart and by who reviews whom",
    setup: "",
    minutes: 20,
  },
  M07: {
    phase: "team",
    purpose: "Create the team homes that own goals, boards and sessions",
    screens: "Spaces, Spaces / <space>",
    who: "Admin, Priya, Amara, Jonas",
    flow: ["Admin creates Product, Sales and Customer Success", "Priya adds members with roles", "A member joins and leaves", "A non-manager cannot manage", "Change settings, archive a spare space"],
    needs: ["M06"],
    gives: "Spaces that goals, the board and the weekly session belong to",
    setup: "",
    minutes: 20,
  },
  M08: {
    phase: "plan",
    purpose: "Walk the first three phases so drafting has something to stand on",
    screens: "Cycle (phases 1 to 3), Cycles card",
    who: "Admin, Priya",
    flow: ["Read the eight-phase rail", "Create the next cycle and set the publication deadline", "Phase 1: sponsor, facilitator, input pack", "Phase 2: rank three strategic issues", "Phase 3: choose a priority"],
    needs: ["M07"],
    gives: "A priority for objectives to contribute to, and the deadline gate 6 checks",
    setup: "",
    minutes: 25,
  },
  M09: {
    phase: "plan",
    purpose: "Draft objectives and key results while the quality checks fire live",
    screens: "Cycle, phase 4; /method/<rule>",
    who: "Sara, Daniel, Tomas",
    flow: ["Draft Sara's objective with champion and reviewer", "Type weak wording and watch OBJ-1 and OBJ-2 fire", "Open a rule card and the method page", "Add key results, weak then measurable", "Record a current value", "Daniel and Tomas draft their own"],
    needs: ["M08"],
    gives: "A draft set of objectives for three teams",
    setup: "AI off",
    minutes: 40,
  },
  M10: {
    phase: "plan",
    purpose: "Work on one goal: values, confidence, comments, documents and files",
    screens: "Goals, Goals / <goal>, Documents / <doc>",
    who: "Sara, Priya, Mei, Jonas",
    flow: ["Open the goal and read its header", "Update values and confidence", "Comment and watch", "Start a private document, then publish two versions", "Attach and detach a file", "Filter the explorer"],
    needs: ["M09"],
    gives: "A goal with history, discussion and a document",
    setup: "A small PDF to attach",
    minutes: 30,
  },
  M11: {
    phase: "plan",
    purpose: "Align the set and publish it through the six gates",
    screens: "Goals, Alignment studio; Cycle, phase 5",
    who: "Priya, Admin",
    flow: ["Look at the cascade", "Link a team goal to the company goal", "Record and confirm a dependency", "Read each gate and follow its fix", "Publish, or override with a reason", "Check a member cannot publish"],
    needs: ["M10"],
    gives: "A published set. The cycle moves to phase 6 and check-ins start",
    setup: "",
    minutes: 30,
  },
  M12: {
    phase: "run",
    purpose: "Track KPIs continuously and recover one that falls out of its corridor",
    screens: "KPIs grid, KPIs / <kpi>, Trees, Recovery board",
    who: "Priya, Admin",
    flow: ["Add a KPI", "Enter values and watch green, amber and red", "Build a formula and a tree", "Let a KPI drop to red", "Launch a recovery objective", "Move a threshold and see colours change"],
    needs: ["M11"],
    gives: "A recovering KPI with its own objective",
    setup: "",
    minutes: 30,
  },
  M13: {
    phase: "run",
    purpose: "Plan the work behind key results and move it on the board",
    screens: "Initiatives, Initiatives / <one>, Board, Tasks / <task>",
    who: "Mei, Sara, Priya, Admin",
    flow: ["Add an initiative and link it to a key result", "Add tasks on the board", "Drag cards between columns", "Watch a second browser update live", "Break a task into a checklist", "See the warning when done work moves nothing"],
    needs: ["M11"],
    gives: "Work linked to key results, which the weekly session reads",
    setup: "Two browsers side by side",
    minutes: 30,
  },
  M14: {
    phase: "run",
    purpose: "Report progress every week and have the reviewer acknowledge it",
    screens: "Check in, Review, Inbox",
    who: "Sara, Priya, Mei, Jonas",
    flow: ["Sara opens what is due", "A check-in without a narrative is refused", "Sara posts one", "Others cannot post or acknowledge", "Priya acknowledges from Review", "Inbox: read, snooze, mute", "A stale goal turns Outdated"],
    needs: ["M11"],
    gives: "Check-ins and confidence the weekly session starts from",
    setup: "Check-in frequency set to daily so a goal is due today",
    minutes: 30,
  },
  M15: {
    phase: "run",
    purpose: "Run the four-step weekly session the product facilitates",
    screens: "Sessions, Session / <id>",
    who: "Admin (facilitator), Priya, Sara, Mei",
    flow: ["Start the template's weekly session", "Confidence round: vote, reveal, confirm", "A low key result needs a blocker", "Resolve and reassign the blocker", "Set two or three commitments", "Digest and close"],
    needs: ["M14"],
    gives: "A finished session with blockers and commitments on record",
    setup: "Priya and Sara are members of the session's space",
    minutes: 30,
  },
  M16: {
    phase: "run",
    purpose: "Check the two agents nudge, propose and stay quiet when they should",
    screens: "Admin: Agents and runs, Nudge volume; Account, Where to reach you",
    who: "Admin, Sara",
    flow: ["Confirm both agents propose by default", "Run the Champion and the Coach now", "Apply and dismiss proposals", "Try sandbox and direct write", "A nudge reaches Sara once a day only", "Quiet hours and a switched-off rule hold nudges"],
    needs: ["M14"],
    gives: "Proof the agents are safe with AI off",
    setup: "",
    minutes: 30,
  },
  M17: {
    phase: "close",
    purpose: "Check the overview screens, search and basic usability",
    screens: "Work map, Search, Activity, copilot panel",
    who: "Priya, Jonas, Admin, Sara",
    flow: ["Read the work map strip and table", "Search finds only what you may open", "Page through Activity", "Open the copilot with AI off", "Check in with the keyboard only", "Use the app at phone width"],
    needs: ["M15"],
    gives: "A read of the whole workspace as a member sees it",
    setup: "A phone, or a 375px browser window",
    minutes: 20,
  },
  M18: {
    phase: "close",
    purpose: "Close goals and the quarter, and record the result",
    screens: "Goals / <goal>, Cycle phase 7, Scorecard, Cycles card",
    who: "Sara, Admin",
    flow: ["Closing a goal needs a retrospective", "Close and reopen a goal", "Score every key result in phase 7", "Record the result on the scorecard", "Archive the old cycle", "Look for the quarterly review"],
    needs: ["M15"],
    gives: "A closed cycle and a scorecard row",
    setup: "",
    minutes: 25,
  },
  M19: {
    phase: "close",
    purpose: "Check nobody sees or does more than their access allows",
    screens: "Admin screens, any goal URL, People / <person>",
    who: "Jonas, Amara, Admin",
    flow: ["A member opening Admin gets Not Found", "Signed-out visitors go to sign-in", "A hidden goal reads as not found", "Convert a member to a guest", "Erase a spare member"],
    needs: ["M11"],
    gives: "Access control checked from the outside",
    setup: "One spare invited account for erasing",
    minutes: 15,
  },
  M20: {
    phase: "connect",
    purpose: "Turn AI on and check it drafts while a person decides",
    screens: "Admin, AI; drafting surface; copilot",
    who: "Admin, Sara, Jonas",
    flow: ["Store a provider key", "A bad key is marked invalid", "AI buttons appear", "A draft is proposed, not saved", "The copilot answers with sources", "Remove the key and the rules still fire"],
    needs: ["M11"],
    gives: "AI checked on and off",
    setup: "A provider key with a spending limit",
    minutes: 30,
  },
  M21: {
    phase: "connect",
    purpose: "Deliver mail and chat messages to real inboxes and apps",
    screens: "Admin, Channels; Account, Where to reach you",
    who: "Admin, Sara, Jonas",
    flow: ["Real mail arrives", "Connect Slack and link Sara", "A nudge arrives in Slack", "Chat commands work", "Repeat for Teams, WhatsApp, Telegram", "Disconnect a channel"],
    needs: ["M16"],
    gives: "Proof that reminders leave the product",
    setup: "SMTP or Mailpit. A public URL and an app per chat provider",
    minutes: 60,
  },
  M22: {
    phase: "connect",
    purpose: "Sign in through a company identity provider and sync the directory",
    screens: "Admin: Single sign-on, Directory sync, General",
    who: "Admin, Jonas",
    flow: ["Add an OIDC provider and restart", "Sign in with it", "Enforce SSO for a domain", "Try SAML", "Push a user through SCIM", "Require a second factor for everyone"],
    needs: ["M04"],
    gives: "Company sign-in checked",
    setup: "An identity provider account. Server access to restart",
    minutes: 45,
  },
  M23: {
    phase: "connect",
    purpose: "Bring data in from spreadsheets and FlowyTeam, and take it out",
    screens: "Admin, Import",
    who: "Admin",
    flow: ["Import goals from CSV", "Import the same file again: nothing new", "A bad row is reported", "Import KPI records from xlsx", "Export the workspace", "FlowyTeam import from the command line"],
    needs: ["M07"],
    gives: "Import and export checked",
    setup: "Sample CSV and xlsx files. A FlowyTeam MySQL copy for the last case",
    minutes: 30,
  },
  M24: {
    phase: "connect",
    purpose: "Reach the product from scripts, the command line and outside AI agents",
    screens: "Account: API tokens, Device, Connected agents; /oauth/authorize",
    who: "Sara",
    flow: ["Create and revoke a token", "Call the API with it", "Log the command line in by device approval", "Connect an MCP agent and ask it for goals", "Revoke the agent"],
    needs: ["M11"],
    gives: "The outside surfaces checked",
    setup: "The okr command line. An MCP client. A public HTTPS URL",
    minutes: 30,
  },
  M25: {
    phase: "connect",
    purpose: "Check the audit trail recorded the sensitive actions from earlier modules",
    screens: "Admin, Audit trail; Admin, General, State",
    who: "Admin, Sara",
    flow: ["Find the suspend and the publish override", "Verify the chain", "Export a date range", "Set the workspace read only, then active again"],
    needs: ["M11"],
    gives: "A verified audit trail",
    setup: "",
    minutes: 15,
  },
  M26: {
    phase: "connect",
    purpose: "Check the managed-cloud operator console",
    screens: "/operator, /operator/<workspace>, /operator/instance, Admin, Support access",
    who: "Operator, Admin, Sara",
    flow: ["Console is hidden on self-hosted", "Operator lists workspaces", "Suspend and restore a workspace", "Ask for and approve support access", "Show an instance banner"],
    needs: ["M04"],
    gives: "Cloud operations checked",
    setup: "OPENOKR_CLOUD=true and an operator row added by SQL",
    minutes: 30,
  },
};
