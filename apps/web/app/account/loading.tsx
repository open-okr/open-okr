import { SegmentLoading } from "../../lib/segment-loading.tsx";

/** This segment's loading state (P6-G24c). UIUX-PLAN.md §4 asks for one each. */
export default function Loading() {
  return <SegmentLoading />;
}
