{
  "extends": "../config/tsconfig.base.json",
  "compilerOptions": {
    // The flaky command line runs under Node's native type stripping, which
    // requires explicit .ts specifiers.
    "allowImportingTsExtensions": true
  },
  "include": ["src", "test", "bin", "fixtures"]
}
